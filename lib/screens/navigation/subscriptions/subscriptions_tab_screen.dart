import 'package:flutter/material.dart';

class SubscriptionTabScreen extends StatelessWidget {
  const SubscriptionTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Subscriptions Tab screen'),
    );
  }
}
